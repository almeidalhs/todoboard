version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "进入前端目录..."
      - cd frontend
      - npm install
  build:
    commands:
      - npm run build
artifacts:
  files:
    - "**/*"
  base-directory: frontend/build